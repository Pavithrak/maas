<%= form_for(mock_server) do |f| %>
  <% if mock_server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mock_server.errors.count, "error") %> prohibited this mock_server from being saved:</h2>

      <ul>
      <% mock_server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
